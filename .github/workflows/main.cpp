#include <iostream>

int main() {
    int a, b;
    
    cout << "Enter two numbers: ";
    cin >> a >> b;
    
    cout << "Addition: " << (a + b) << std::endl;
    cout << "Subtraction: " << (a - b) << std::endl;

    return 0;
}
