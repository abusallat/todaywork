#include <iostream>

int main() {
    int a, b;
    
    cout << "Enter two numbers: ";
    cin >> a >> b;
    
    cout << "Addition: " << (a + b) << endl;
    cout << "Subtraction: " << (a - b) << endl;

    return 0;
}
